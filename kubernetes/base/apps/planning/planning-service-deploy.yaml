apiVersion: apps/v1
kind: Deployment
metadata:
  name: planning-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: planning-service
  template:
    metadata:
      labels:
        app: planning-service
    spec:
      containers:
      - name: planning-service
        image: planning-service:latest 
        imagePullPolicy: Never 
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: traveling-helper-config
        - secretRef:
            name: traveling-helper-secret
        env:
        - name: DB_URL
          value: "jdbc:mysql://mysql-service:3306/planning_prod"
        # Health Check
        readinessProbe:
          httpGet:
            path: /api/plans/health
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10